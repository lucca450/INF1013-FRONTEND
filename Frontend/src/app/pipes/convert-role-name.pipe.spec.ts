import { ConvertRoleNamePipe } from './convert-role-name.pipe';

describe('ConvertRoleNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertRoleNamePipe();
    expect(pipe).toBeTruthy();
  });
});

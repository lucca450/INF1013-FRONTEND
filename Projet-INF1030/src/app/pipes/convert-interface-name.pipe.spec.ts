import { ConvertInterfaceName } from './convert-interface-name.pipe';

describe('ConvertInterfaceNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertInterfaceName();
    expect(pipe).toBeTruthy();
  });
});

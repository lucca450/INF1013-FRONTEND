import { ConvertInterfaceNamePipe } from './convert-interface-name.pipe';

describe('ConvertInterfaceNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertInterfaceNamePipe();
    expect(pipe).toBeTruthy();
  });
});
